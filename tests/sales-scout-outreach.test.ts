import assert from "node:assert/strict";
import test from "node:test";
import { buildManualHandoff,generateDeterministicOutreachDraft,nextFollowUpAt,nextOutreachSequence,recommendOutreachChannels } from "../src/lib/sales-scout/outreach.ts";

const facts={businessName:"Example Hotel",businessCategory:"Hotel",city:"Lagos",state:"Lagos",productScope:"fresh chicken and vegetables",deliverySummary:"Delivery depends on confirmed logistics"};
test("deterministic initial draft contains no fabricated commercial promises",()=>{const draft=generateDeterministicOutreachDraft({sequenceNumber:1,...facts});assert.match(draft,/Shields Farms/);assert.match(draft,/who handles produce purchasing/i);assert.match(draft,/quantity, destination and logistics confirmation/i);assert.doesNotMatch(draft,/guaranteed|minimum order|in stock|free delivery|buyer name/i);});
test("follow-ups stop after sequence three and use 3/4 day cadence",()=>{assert.equal(nextOutreachSequence([]),1);assert.equal(nextOutreachSequence([{sequence_number:1,status:"sent"}]),2);assert.equal(nextOutreachSequence([{sequence_number:1,status:"sent"},{sequence_number:2,status:"no_response"}]),3);assert.equal(nextOutreachSequence([{sequence_number:1,status:"sent"},{sequence_number:2,status:"sent"},{sequence_number:3,status:"sent"}]),null);const sent=new Date("2026-08-02T00:00:00Z");assert.equal(nextFollowUpAt(1,sent)?.toISOString(),"2026-08-05T00:00:00.000Z");assert.equal(nextFollowUpAt(2,sent)?.toISOString(),"2026-08-06T00:00:00.000Z");assert.equal(nextFollowUpAt(3,sent),null);});
test("channel recommendation and handoffs are safely encoded",()=>{const channels=recommendOutreachChannels([{id:"1",platform:"email",handleOrValue:"sales@example.com",profileUrl:null,active:true},{id:"2",platform:"whatsapp",handleOrValue:"07032821293",profileUrl:null,active:true},{id:"3",platform:"website",handleOrValue:"https://example.com",profileUrl:"https://example.com",active:true}]);assert.deepEqual(channels.map(item=>item.platform),["whatsapp","email"]);const message="Hello & welcome?";assert.match(buildManualHandoff({platform:"whatsapp",value:"07032821293",profileUrl:null,message})??"",/text=Hello%20%26%20welcome%3F/);assert.match(buildManualHandoff({platform:"email",value:"sales@example.com",profileUrl:null,message})??"",/^mailto:/);assert.match(buildManualHandoff({platform:"phone",value:"07032821293",profileUrl:null,message})??"",/^tel:/);});
