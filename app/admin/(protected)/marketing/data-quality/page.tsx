import {AdminHeader} from "@/src/components/admin";import {MarketingNav} from "@/src/components/marketing-command-ui";import {requireAdmin} from "@/src/lib/admin-auth";import {createAdminSupabaseClient} from "@/src/lib/supabase/admin";import {ga4Configuration,getGa4Summary} from "@/src/lib/ga4-data";import {googleIntegration} from "@/src/lib/analytics";export const dynamic="force-dynamic";
export default async function Page(){await requireAdmin();const db=createAdminSupabaseClient();const[campaigns,clicks,orders,offers,ga]=await Promise.all([db.from("marketing_campaigns").select("id,slug,target_path"),db.from("marketing_campaign_clicks").select("id,campaign_id").limit(1),db.from("orders").select("id,order_reference,payment_status,first_touch_attribution,last_touch_attribution").eq("payment_status","paid").limit(2000),db.from("affiliate_offers").select("id,is_active").limit(1000),getGa4Summary()]);const paid=orders.data??[];const checks=[["Campaign schema readable",!campaigns.error],["Campaign redirect clicks observed",(clicks.data?.length??0)>0],["Campaign UTM IDs available",(campaigns.data??[]).every((x)=>Boolean(x.slug))],["First-touch attribution observed",paid.some((x)=>x.first_touch_attribution)],["Last-touch attribution observed",paid.some((x)=>x.last_touch_attribution)],["Paid-order attribution query functional",!orders.error],["GA measurement ID valid",googleIntegration().valid],["GA Data API",ga4Configuration().configured?"Configured":"Intentionally absent"],["Recent page views via API",ga.available?Number(ga.metrics.screenPageViews||0)>0:"Unavailable"],["Invalid campaign target paths",(campaigns.data??[]).filter((x)=>!x.target_path?.startsWith("/")||x.target_path.startsWith("//")).length===0],["Orders without attribution",paid.filter((x)=>!x.first_touch_attribution&&!x.last_touch_attribution).length],["Unavailable affiliate offers",(offers.data??[]).filter((x)=>!x.is_active).length]];return <><AdminHeader title="Marketing data quality" body="Safe production checks. Revenue remains sourced from paid Supabase orders, never GA."/><MarketingNav/><div className="rounded-xl border bg-white">{checks.map(([label,value])=><div key={String(label)} className="flex items-center justify-between gap-4 border-b p-4"><strong>{String(label)}</strong><span>{typeof value==="boolean"?(value?"Ready":"Attention"):String(value)}</span></div>)}</div><section className="mt-6 rounded-xl border bg-white p-5"><h2 className="font-bold">Non-revenue production test</h2><ol className="mt-3 list-decimal space-y-2 pl-5 text-sm"><li>Open a campaign’s /go/slug URL in a private browser.</li><li>Confirm redirect target and utm_id equal the campaign slug.</li><li>Confirm one redirect click appears here.</li><li>Submit a test Contact or Business Supply inquiry, clearly marked test, then remove it.</li><li>Use GA DebugView for page_view and safe test events; do not create or mark an order paid.</li></ol></section></>}
