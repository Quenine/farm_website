import Link from "next/link";import {notFound} from "next/navigation";import {AdminHeader} from "@/src/components/admin";import {MarketingNav} from "@/src/components/marketing-command-ui";import {SocialActivityManager} from "@/src/components/social-activity-manager";import {requireAdmin} from "@/src/lib/admin-auth";import {createAdminSupabaseClient} from "@/src/lib/supabase/admin";export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{id:string}>}){await requireAdmin();const{id}=await params;const result=await createAdminSupabaseClient().from("marketing_social_activities").select("*").eq("id",id).maybeSingle();if(!result.data)notFound();return <><AdminHeader title={`${result.data.platform} social activity`} body="Metrics are manually reported platform values and are not independently verified."/><MarketingNav/><Link href="/admin/marketing/social" className="mb-4 inline-flex h-11 items-center font-bold text-green-800">Back to social activity</Link><SocialActivityManager activity={result.data}/></>}
