"use client";
import {useEffect,useState} from "react";
import {getConsentPreferences,trackSafeEvent} from "@/src/lib/analytics";
type Status={consent:boolean;mounted:boolean;script:boolean;dataLayer:boolean;path:string;last:string};
function inspect():Status{let last="None";try{const parsed=JSON.parse(sessionStorage.getItem("farm_last_safe_analytics_event")||"null") as {name?:string}|null;last=parsed?.name||"None"}catch{}return{consent:Boolean(getConsentPreferences()?.analytics),mounted:document.documentElement.dataset.googleAnalytics==="mounted",script:Boolean(document.querySelector('script[src*="googletagmanager.com/gtag/js"]')),dataLayer:Array.isArray(window.dataLayer),path:window.location.pathname+window.location.search,last}}
export function AnalyticsTestClient(){const[status,setStatus]=useState<Status|null>(null),[confirmation,setConfirmation]=useState("");useEffect(()=>{const refresh=()=>setStatus(inspect());queueMicrotask(refresh);const timer=setInterval(refresh,500);window.addEventListener("farm-analytics-event",refresh);return()=>{clearInterval(timer);window.removeEventListener("farm-analytics-event",refresh)}},[]);if(!status)return <p className="mt-5">Checking browser state…</p>;return <section className="mt-6 rounded-xl border bg-white p-5"><dl className="grid gap-3 text-sm">{Object.entries(status).map(([key,value])=><div key={key} className="flex justify-between gap-4 border-b pb-2"><dt className="font-bold capitalize">{key}</dt><dd className="break-all text-right">{typeof value==="boolean"?(value?"Yes":"No"):value}</dd></div>)}</dl><button type="button" onClick={()=>{trackSafeEvent("shields_tag_test",{source:"analytics_test"});setConfirmation("Safe test event dispatched locally.")}} className="mt-5 min-h-11 rounded-full bg-green-800 px-5 font-bold text-white">Fire test event</button><p aria-live="polite" className="mt-3 text-sm text-green-900">{confirmation}</p></section>}
