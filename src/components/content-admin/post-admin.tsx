"use client";

import Link from "next/link";
import { useMemo, useState, useTransition } from "react";
import { savePostAction } from "@/app/admin/(protected)/content/actions";
import type { AdminRecord } from "@/src/lib/content-admin";
import { formatNaira } from "@/src/lib/format";

type Options = { authors: AdminRecord[]; categories: AdminRecord[]; tags: AdminRecord[]; sources: AdminRecord[]; products: AdminRecord[]; offers: AdminRecord[] };

function slugify(value: string) { return value.toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, ""); }
function value(record: AdminRecord | null, key: string, fallback = "") { const item = record?.[key]; return item === null || item === undefined ? fallback : String(item); }
function selectedIds(record: AdminRecord | null, key: string, childKey: string) { return Array.isArray(record?.[key]) ? (record?.[key] as AdminRecord[]).map((item) => String(item[childKey])).filter(Boolean) : []; }

export function PostsList({ posts }: { posts: AdminRecord[] }) {
  const [search, setSearch] = useState("");
  const [status, setStatus] = useState("all");
  const [format, setFormat] = useState("all");
  const filtered = useMemo(() => posts.filter((post) => {
    const q = search.trim().toLowerCase();
    if (q && ![post.title, post.slug, post.excerpt].join(" ").toLowerCase().includes(q)) return false;
    if (status !== "all" && post.status !== status) return false;
    if (format !== "all" && post.content_format !== format) return false;
    return true;
  }), [format, posts, search, status]);
  return <div className="grid gap-5"><div className="rounded-lg bg-white p-5 shadow-sm"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><h2 className="text-2xl font-bold text-green-950">Posts</h2><p className="mt-1 text-sm text-stone-600">{filtered.length} of {posts.length} articles shown.</p></div><Link href="/admin/content/posts/new" className="rounded-full bg-green-800 px-5 py-3 text-sm font-bold text-white">Create Article</Link></div><div className="mt-4 grid gap-3 md:grid-cols-3"><input value={search} onChange={(event)=>setSearch(event.target.value)} placeholder="Search title, slug, excerpt" className="h-11 rounded-lg border border-stone-200 px-4 text-sm" /><select value={status} onChange={(event)=>setStatus(event.target.value)} className="h-11 rounded-lg border border-stone-200 px-4 text-sm"><option value="all">All statuses</option><option value="draft">Draft</option><option value="review">Review</option><option value="published">Published</option><option value="archived">Archived</option></select><select value={format} onChange={(event)=>setFormat(event.target.value)} className="h-11 rounded-lg border border-stone-200 px-4 text-sm"><option value="all">All formats</option><option value="article">Article</option><option value="video_companion">Video</option><option value="comparison">Comparison</option><option value="resource_guide">Resource</option><option value="case_study">Case study</option><option value="farm_field_note">Field note</option></select></div></div>{filtered.length ? <div className="overflow-hidden rounded-lg bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[980px] text-left text-sm"><thead className="bg-green-950 text-white"><tr>{["Title","Status","Category","Author","Format","Audience","Affiliate","Updated","Published","Actions"].map((h)=><th key={h} className="px-4 py-3 font-semibold">{h}</th>)}</tr></thead><tbody className="divide-y divide-stone-100">{filtered.map((post)=><tr key={String(post.id)}><td className="px-4 py-4 font-bold text-green-950">{String(post.title)}</td><td className="px-4 py-4">{String(post.status)}</td><td className="px-4 py-4">{relationName(post.content_categories)}</td><td className="px-4 py-4">{relationName(post.content_authors)}</td><td className="px-4 py-4">{String(post.content_format).replaceAll("_"," ")}</td><td className="px-4 py-4">{String(post.audience_scope)}</td><td className="px-4 py-4">{post.contains_affiliate_content ? "Yes" : "No"}</td><td className="px-4 py-4">{dateLabel(post.updated_at)}</td><td className="px-4 py-4">{dateLabel(post.published_at)}</td><td className="px-4 py-4"><div className="flex flex-wrap gap-2"><Link href={`/admin/content/posts/${post.id}/edit`} className="rounded-full bg-green-50 px-3 py-1 text-xs font-bold text-green-800">Edit</Link>{post.status === "published" ? <Link href={`/blog/${post.slug}`} className="rounded-full bg-stone-100 px-3 py-1 text-xs font-bold text-stone-700">View</Link> : <Link href={`/admin/content/posts/${post.id}/edit?preview=1`} className="rounded-full bg-stone-100 px-3 py-1 text-xs font-bold text-stone-700">Preview</Link>}</div></td></tr>)}</tbody></table></div></div> : <div className="rounded-lg bg-white p-8 text-center shadow-sm"><h3 className="text-xl font-bold text-green-950">No articles yet</h3><p className="mt-2 text-sm text-stone-600">Create a draft article, attach sources, products and affiliate offers, then publish explicitly when ready.</p><Link href="/admin/content/posts/new" className="mt-4 inline-flex rounded-full bg-green-800 px-5 py-2 text-sm font-bold text-white">Create Article</Link></div>}</div>;
}

function relationName(value: unknown) { const row = Array.isArray(value) ? value[0] as AdminRecord | undefined : value as AdminRecord | undefined; return String(row?.name ?? "-"); }
function dateLabel(value: unknown) { return typeof value === "string" && value ? new Date(value).toLocaleDateString("en-NG") : "-"; }

export function PostEditor({ post, options }: { post: AdminRecord | null; options: Options }) {
  const [draft, setDraft] = useState<AdminRecord>(post ?? { status: "draft", content_format: "article", post_type: "guide", audience_scope: "nigeria", is_featured: false, contains_affiliate_content: false, title: "", slug: "", excerpt: "", content_markdown: "" });
  const [tagIds, setTagIds] = useState(selectedIds(post, "content_post_tags", "tag_id"));
  const [sourceIds, setSourceIds] = useState(selectedIds(post, "content_post_sources", "source_id"));
  const [productLinks, setProductLinks] = useState<Array<{product_id:string; sort_order:number; custom_context:string}>>(Array.isArray(post?.content_post_products) ? (post?.content_post_products as AdminRecord[]).map((item, index)=>({ product_id:String(item.product_id), sort_order:Number(item.sort_order ?? index + 1), custom_context:String(item.custom_context ?? "") })) : []);
  const [offerIds, setOfferIds] = useState(selectedIds(post, "content_post_affiliate_offers", "offer_id"));
  const [message, setMessage] = useState<string | null>(null);
  const [isPending, startTransition] = useTransition();
  const [dirty, setDirty] = useState(false);

  const set = (key: string, next: unknown) => { setDraft((current) => ({ ...current, [key]: next, ...(key === "title" && !current.slug ? { slug: slugify(String(next)) } : {}) })); setDirty(true); };
  const insert = (token: string) => set("content_markdown", `${value(draft,"content_markdown")}\n\n${token}\n`);
  const submit = (action: "draft" | "continue" | "review" | "publish" | "unpublish" | "archive") => {
    const payload = { ...draft, id: post?.id, action, tag_ids: tagIds, source_ids: sourceIds, product_links: productLinks.filter((item)=>item.product_id), offer_links: offerIds.map((offer_id, index)=>({ offer_id, sort_order:index + 1 })) };
    startTransition(async () => { const result = await savePostAction(payload); setMessage(result.message); if (result.success) { setDirty(false); if (action === "continue" && result.id && !post?.id) window.location.href = `/admin/content/posts/${result.id}/edit`; } });
  };
  const selectedProducts = productLinks.map((link) => ({ link, product: options.products.find((product) => product.id === link.product_id) })).filter((item) => item.product);
  return <div className="grid gap-6"><div className="rounded-lg bg-white p-5 shadow-sm"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><h2 className="text-2xl font-bold text-green-950">{post ? "Edit Article" : "Create Article"}</h2><p className="mt-1 text-sm text-stone-600">Publishing is explicit. Drafts and previews remain protected and noindex.</p>{dirty ? <p className="mt-1 text-sm font-bold text-amber-700">Unsaved changes</p> : null}</div><div className="flex flex-wrap gap-2"><button onClick={()=>submit("draft")} disabled={isPending} className="rounded-full border border-green-800 px-4 py-2 text-sm font-bold text-green-950">Save Draft</button><button onClick={()=>submit("continue")} disabled={isPending} className="rounded-full border border-green-800 px-4 py-2 text-sm font-bold text-green-950">Save and Continue</button><button onClick={()=>submit("review")} disabled={isPending} className="rounded-full bg-amber-600 px-4 py-2 text-sm font-bold text-white">Send to Review</button><button onClick={()=>submit("publish")} disabled={isPending} className="rounded-full bg-green-800 px-4 py-2 text-sm font-bold text-white">Publish</button>{post ? <button onClick={()=>submit("archive")} disabled={isPending} className="rounded-full bg-stone-800 px-4 py-2 text-sm font-bold text-white">Archive</button> : null}</div></div>{message ? <p className="mt-4 rounded-lg bg-green-50 p-3 text-sm font-bold text-green-900">{message}</p> : null}</div><section className="grid gap-4 rounded-lg bg-white p-5 shadow-sm md:grid-cols-2"><Input label="Title *" value={value(draft,"title")} onChange={(v)=>set("title",v)} /><Input label="Slug *" value={value(draft,"slug")} onChange={(v)=>set("slug",v)} /><Text label="Excerpt *" value={value(draft,"excerpt")} onChange={(v)=>set("excerpt",v)} /><Text label="Answer summary" value={value(draft,"answer_summary")} onChange={(v)=>set("answer_summary",v)} /><Text label="Key takeaways (one per line)" value={Array.isArray(draft.key_takeaways) ? (draft.key_takeaways as string[]).join("\n") : value(draft,"key_takeaways")} onChange={(v)=>set("key_takeaways",v)} /><Select label="Author *" value={value(draft,"author_id")} onChange={(v)=>set("author_id",v)} options={options.authors.map((item): [string, string] => [String(item.id),String(item.name)])} /><Select label="Category *" value={value(draft,"category_id")} onChange={(v)=>set("category_id",v)} options={options.categories.map((item): [string, string] => [String(item.id),String(item.name)])} /><Multi label="Tags" items={options.tags} selected={tagIds} onChange={setTagIds} /><Select label="Content format" value={value(draft,"content_format","article")} onChange={(v)=>set("content_format",v)} options={[["article","Article"],["video_companion","Video companion"],["comparison","Comparison"],["resource_guide","Resource guide"],["case_study","Case study"],["farm_field_note","Farm field note"]]} /><Select label="Post type" value={value(draft,"post_type","guide")} onChange={(v)=>set("post_type",v)} options={[["guide","Guide"],["tutorial","Tutorial"],["buying_guide","Buying guide"],["review","Review"],["comparison","Comparison"],["case_study","Case study"],["market_insight","Market insight"],["farm_update","Farm update"]]} /><Select label="Audience" value={value(draft,"audience_scope","nigeria")} onChange={(v)=>set("audience_scope",v)} options={[["nigeria","Nigeria"],["africa","Africa"],["global","Global"]]} /><Input label="Featured image URL" value={value(draft,"featured_image_url")} onChange={(v)=>set("featured_image_url",v)} /><Input label="Featured image alt text" value={value(draft,"featured_image_alt")} onChange={(v)=>set("featured_image_alt",v)} /></section><section className="rounded-lg bg-white p-5 shadow-sm"><div className="flex flex-wrap gap-2"><button onClick={()=>insert("## Heading")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Heading</button><button onClick={()=>insert("**bold text**")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Bold</button><button onClick={()=>insert("_italic text_")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Italic</button><button onClick={()=>insert("[link text](https://example.com)")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Link</button><button onClick={()=>insert("- List item")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">List</button><button onClick={()=>insert("> Quote")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Quote</button><button onClick={()=>insert("| Column | Column |\n| --- | --- |\n| Value | Value |") } className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Table</button><button onClick={()=>insert("[[comparison:post-offers]]")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Comparison</button><button onClick={()=>insert("[[video:post-video]]")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Video</button><button onClick={()=>insert("[[sources]]")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Sources</button><button onClick={()=>insert("[[newsletter]]")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Newsletter</button><button onClick={()=>insert("[[callout:business-supply]]")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Business Supply CTA</button><button onClick={()=>insert("[[tool:poultry-feed-requirement]]")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Feed Tool</button><button onClick={()=>insert("[[tool:egg-sales-margin]]")} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Egg Tool</button></div><Text label="Markdown content *" value={value(draft,"content_markdown")} onChange={(v)=>set("content_markdown",v)} rows={16} /></section><section className="grid gap-4 rounded-lg bg-white p-5 shadow-sm"><h3 className="text-xl font-bold text-green-950">Sources</h3><Multi label="Sources" items={options.sources} selected={sourceIds} onChange={setSourceIds} labelKey="title" /></section><section className="grid gap-4 rounded-lg bg-white p-5 shadow-sm"><h3 className="text-xl font-bold text-green-950">Related Shields Farms Products</h3><p className="text-sm text-stone-600">Product blocks use live product data. Do not copy prices into Markdown.</p><Select label="Add product" value="" onChange={(id)=>id && !productLinks.some((link)=>link.product_id===id) && setProductLinks([...productLinks,{product_id:id,sort_order:productLinks.length+1,custom_context:""}])} options={[["","Choose product"],...options.products.map((item): [string, string] => [String(item.id),String(item.name)])]} />{selectedProducts.map(({link,product})=><div key={link.product_id} className="rounded-lg border border-stone-200 p-4"><p className="font-bold text-green-950">{String(product?.name)} - {formatNaira(Number(product?.price ?? 0))} / {String(product?.unit ?? "unit")}</p><Input label="Editorial context" value={link.custom_context} onChange={(v)=>setProductLinks(productLinks.map((item)=>item.product_id===link.product_id ? {...item,custom_context:v}:item))} /><div className="mt-2 flex gap-2"><button onClick={()=>insert(`[[product:${String(product?.slug)}]]`)} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Insert product token</button><button onClick={()=>setProductLinks(productLinks.filter((item)=>item.product_id!==link.product_id))} className="rounded-full border border-green-800 px-3 py-1 text-xs font-bold text-green-950 hover:bg-green-50">Remove</button></div></div>)}</section><section className="grid gap-4 rounded-lg bg-white p-5 shadow-sm"><h3 className="text-xl font-bold text-green-950">Affiliate and SEO</h3><label className="flex gap-3 text-sm font-bold"><input type="checkbox" checked={draft.contains_affiliate_content === true} onChange={(e)=>set("contains_affiliate_content",e.target.checked)} /> Contains affiliate content</label><label className="flex gap-3 text-sm font-bold"><input type="checkbox" checked={draft.is_featured === true} onChange={(e)=>set("is_featured",e.target.checked)} /> Featured</label><Multi label="Related affiliate offers" items={options.offers} selected={offerIds} onChange={setOfferIds} labelKey="title" /><Text label="Recommendation methodology" value={value(draft,"recommendation_methodology")} onChange={(v)=>set("recommendation_methodology",v)} /><Text label="Affiliate disclosure" value={value(draft,"custom_affiliate_disclosure")} onChange={(v)=>set("custom_affiliate_disclosure",v)} /><Input label="SEO title" value={value(draft,"seo_title")} onChange={(v)=>set("seo_title",v)} /><Text label="SEO description" value={value(draft,"seo_description")} onChange={(v)=>set("seo_description",v)} /><Input label="External canonical URL" value={value(draft,"external_canonical_url")} onChange={(v)=>set("external_canonical_url",v)} /><Input label="Publication date/time" type="datetime-local" value={value(draft,"published_at").slice(0,16)} onChange={(v)=>set("published_at",v)} /><Select label="Status" value={value(draft,"status","draft")} onChange={(v)=>set("status",v)} options={[["draft","Draft"],["review","Review"],["published","Published"],["archived","Archived"]]} /></section></div>;
}

function Input({ label, value, onChange, type="text" }: { label:string; value:string; type?:string; onChange:(value:string)=>void }) { return <label className="grid gap-2 text-sm font-bold text-stone-800">{label}<input type={type} value={value} onChange={(e)=>onChange(e.target.value)} className="h-11 rounded-lg border border-stone-200 px-4 text-sm" /></label>; }
function Text({ label, value, onChange, rows=4 }: { label:string; value:string; rows?:number; onChange:(value:string)=>void }) { return <label className="grid gap-2 text-sm font-bold text-stone-800 md:col-span-2">{label}<textarea rows={rows} value={value} onChange={(e)=>onChange(e.target.value)} className="rounded-lg border border-stone-200 px-4 py-3 text-sm" /></label>; }
function Select({ label, value, onChange, options }: { label:string; value:string; options:Array<[string,string]>; onChange:(value:string)=>void }) { return <label className="grid gap-2 text-sm font-bold text-stone-800">{label}<select value={value} onChange={(e)=>onChange(e.target.value)} className="h-11 rounded-lg border border-stone-200 px-4 text-sm">{options.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label>; }
function Multi({ label, items, selected, onChange, labelKey="name" }: { label:string; items:AdminRecord[]; selected:string[]; labelKey?:string; onChange:(ids:string[])=>void }) { return <fieldset className="grid gap-2 text-sm font-bold text-stone-800 md:col-span-2"><legend>{label}</legend><div className="grid max-h-44 gap-2 overflow-y-auto rounded-lg border border-stone-200 p-3 md:grid-cols-2">{items.map((item)=><label key={String(item.id)} className="flex items-center gap-2 font-medium"><input type="checkbox" checked={selected.includes(String(item.id))} onChange={(e)=>onChange(e.target.checked ? [...selected,String(item.id)] : selected.filter((id)=>id!==String(item.id)))} />{String(item[labelKey] ?? item.slug)}</label>)}</div></fieldset>; }

